name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
	- name: get flex/bison
	  run: apt install flex bison
    - name: make
      run: make
    - name: run tests
      run: ./test/bin/epatest
